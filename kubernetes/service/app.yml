apiVersion: v1

kind: Service

metadata:
  name: service-flask-wsgi
  namespace: ingress-nginx

spec:
  type: ClusterIP
  selector:
    app: flask-wsgi
  ports:
    - name: port-flask-wsgi
      port: 9090
      targetPort: 9090
