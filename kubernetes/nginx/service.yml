apiVersion: v1

kind: Service

metadata:
  name: service-my-nginx

spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: my-nginx
  ports:
    - name: port-my-nginx
      port: 8080
      targetPort: port-flask-wsgi
      nodePort: 30080
