apiVersion: v1

kind: Service

metadata:
  name: service-my-nginx

spec:
  type: NodePort
  externalTrafficPolicy: Local
  selector:
    app: my-nginx
  ports:
    - name: port-my-nginx
      port: 8080
      targetPort: 80
      nodePort: 30080
