apiVersion: v1

kind: Service

metadata:
  name: service-my-nginx

spec:
  type: NodePort
  externalTraffiicPolicy: Local
  selector:
    app: my-nginx
  ports:
    - name: port-my-nginx
      port: 8080
      targetPort: port-flask-wsgi
      nodePort: 30080
